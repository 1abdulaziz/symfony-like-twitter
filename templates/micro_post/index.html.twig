{% extends 'base.html.twig' %}

{% block title %}Hello {{ controller_name }}!{% endblock %}

{% block body %}
    <h1> <a href="{{ path('micro_post_add') }}">Add new post</a> </h1>
    <div class="grid grid-cols-1 sm:grid-cols-2 xl:grid-cols-3 gap-4">
    {% if posts|length > 0 %}
    {% for post in posts %}
        <div class="mb-4">
            <div class="text-2xl">
                <a href="{{ path('micro_post_show', {'post': post.id}) }}">{{ post.title }}</a>
            </div>
            <div class="text-xl dark:text-gray-400">
                <p>{{ post.text }}</p>
            </div>
            <div class="text-gray-500 dark:text-gray-400">
                <p>{{ post.created | date('d/m/Y') }}</p>
            </div>
            <div >
                <a href="#">
                    Like:  <span class="px-2 rounded-md bg-gray-200 dark:bg-gray-700"> 0 </span>
                </a>
            </div>

            <div class="text-sm text-gray-500 dark:text-gray-400">
                <a href="#">User@gmail.com</a>
            </div>

            <div class="text-sm text-gray-500 dark:text-gray-400">
                <a href="{{ path('micro_post_edit', {'post': post.id}) }}">Edit</a>
            </div>
        </div>

{#        #}
{#        <p>{{ post.text }}</p>#}
{#        <p>{{ post.created | date('d/m/Y') }}</p>#}
{#        <ul>#}
{#            <li><a href="{{ path('micro_post_edit', {'post': post.id}) }}">Edit</a></li>#}
{#            <li><a href="{{ path('micro_post_delete', {'post': post.id}) }}">Delete</a></li>#}
{#            <li><a href="{{ path('app_micro_post_comment', {'post': post.id}) }}">comment - {{ post.comments.count }}</a></li>#}
{#        </ul>#}
    {% endfor %}
    {% else %}
        <p>No posts found</p>
    {% endif %}
{% endblock %}


